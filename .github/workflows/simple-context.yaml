name: Simple Context
on: workflow_dispatch
jobs:
  print-context:
    runs-on: ubuntu-latest
    steps:
      - name: Print context
        run: |
          echo "GITHUB_CONTEXT: ${{ toJson(github) }}"
